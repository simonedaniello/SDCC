'use strict';

describe('myApp.workInProgress module', function() {

    beforeEach(module('myApp.workInProgress'));

    describe('workInProgress controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('workInProgressCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});