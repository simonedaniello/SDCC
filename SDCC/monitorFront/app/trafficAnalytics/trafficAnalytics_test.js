'use strict';

describe('myApp.trafficAnalytics module', function() {

    beforeEach(module('myApp.trafficAnalytics'));

    describe('trafficAnalytics controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('trafficAnalyticsCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});