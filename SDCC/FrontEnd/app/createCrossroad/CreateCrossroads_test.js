'use strict';

describe('myApp.createCrossroad module', function() {

  beforeEach(module('myApp.createCrossroad'));

  describe('createCrossroad controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('createCrossroadCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});